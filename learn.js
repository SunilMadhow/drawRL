var  num_episodes = 0;
var w = []

function initParameter() {
	p = getVectorLength(num_actions);
}

function learn() {
	var reward = 0;
	var observation = reset();

	while (true) {

	}
}